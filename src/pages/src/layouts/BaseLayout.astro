---
const { title, description } = Astro.props;
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title ? `${title} | Fishing Blog` : 'Fishing Blog'}</title>
    <meta name="description" content={description || "Catch the latest fishing tips, news, and adventures."} />
    <style>
      :root {
        --primary: #0070f3;
        --accent: #00c49f;
        --background: #f4f7f6;
        --card: #fff;
        --text: #111;
        --secondary: #555;
        --max-width: 1100px;
        --header-height: 70px;
      }

      body {
        margin: 0;
        font-family: 'Helvetica Neue', sans-serif;
        background: var(--background);
        color: var(--text);
      }

      a {
        text-decoration: none;
        color: var(--primary);
      }

      a:hover {
        text-decoration: underline;
      }

      header {
        position: sticky;
        top: 0;
        background: var(--card);
        box-shadow: 0 2px 6px rgba(0,0,0,0.05);
        display: flex;
        justify-content: space-between;
        align-items: center;
        height: var(--header-height);
        padding: 0 2rem;
        z-index: 10;
      }

      header h1 {
        font-size: 1.8rem;
        margin: 0;
      }

      nav a {
        margin-left: 1.5rem;
        font-weight: bold;
      }

      main {
        max-width: var(--max-width);
        margin: calc(var(--header-height) + 1rem) auto 3rem;
        padding: 0 1rem;
      }

      footer {
        text-align: center;
        padding: 2rem 1rem;
        font-size: 0.9rem;
        color: var(--secondary);
      }
    </style>
  </head>
  <body>
    <header>
      <h1><a href="/fishing/blog/">Fishing Blog</a></h1>
      <nav>
        <a href="/fishing/blog/">Home</a>
        <a href="/fishing/blog/">Blog</a>
        <!-- Future: Shop, About -->
      </nav>
    </header>

    <main>
      <slot />
    </main>

    <footer>
      &copy; 2026 Fishing Blog. Built with Astro.
    </footer>
  </body>
</html>
