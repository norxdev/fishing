---
import { getEntryBySlug } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';

const { slug } = Astro.params;
const post = await getEntryBySlug('blog', slug);

if (!post) {
  throw new Error(`Post not found: ${slug}`);
}
---

<BaseLayout title={post.data.title} description={post.data.description}>

  <article class="blog-post">
    <h1>{post.data.title}</h1>
    <p class="date">{new Date(post.data.publishDate).toLocaleDateString()}</p>
    <img src={post.data.image || "https://source.unsplash.com/1200x500/?fishing"} alt={post.data.title} />
    <div class="post-content">
      {post.body ? <post.Content /> : <p>{post.data.description}</p>}
    </div>
    <a href="/fishing" class="back-home">← Back to Home</a>
  </article>

</BaseLayout>
